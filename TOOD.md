# TODO

- logging
- dockerize
- FFMPEG bitrate
- youtube playlists
- soundcloud playlists
- code cleanup (very dirty, break stuff down into functions, no spaghetti code)
- Support for adding only N items from playlist in the `play` command. When a playlist is passed in, the user can additionally provide an extra argument `N` such that when provided, only the first N items of the playlist are queued.
